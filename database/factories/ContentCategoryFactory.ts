
import Factory from '@ioc:Adonis/Lucid/Factory'
import MContentCategory from 'App/Models/MContentCategory'

export default Factory.define(MContentCategory, ({ faker }) => {
  return {
    
  }
}).build()
