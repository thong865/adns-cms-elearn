<div class="bg-white rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
    <a href="{{route('ContentDetail',{id:data?.id})}}">
        <img class="{{ImageClass}} w-full" src="{{data?.background}}" alt="" />
    </a>
    <div class="p-5">
        <a href="{{route('ContentDetail',{id:data?.id})}}">
            <h5 class="mb-2 text-xl font-bold tracking-tight text-gray-900 text-ellipsis overflow-hidden max-h-[50px]">{{data?.title}}</h5>
        </a>
        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400 text-ellipsis overflow-hidden max-h-[70px]">{{{data?.body}}}</p>
    </div>
</div>